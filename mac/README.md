This project creates a ready-to-use sql server on a mac using colima.

# Getting started

1. download Colima using `brew install colima`
2. build this project to create the docker image
3. run the image
